import { SliderLayers } from './slider-layers';

export function SliderContent({ slider }) {
  // ----------------------------------------------------------------------
  return <SliderLayers slider={slider} />;
}
