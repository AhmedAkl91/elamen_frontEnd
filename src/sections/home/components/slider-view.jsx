
import { SliderContent } from './slider';

export function SliderView({ slider }) {
  // ----------------------------------------------------------------------

  return <SliderContent slider={slider} />;
}
